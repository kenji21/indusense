# CLAUDE.md

- Privé : tu ne lis pas ce qui est dans data

## Notebooks Jupyter

- Ne jamais remplacer le contenu d'une cellule existante sans vérification préalable : toujours insérer une nouvelle cellule (`edit_mode=insert`) plutôt que d'écraser (`replace`) une cellule qui contient déjà du code fonctionnel.
